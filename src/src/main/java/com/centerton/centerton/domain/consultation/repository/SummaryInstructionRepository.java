package com.centerton.centerton.domain.consultation.repository;

import com.centerton.centerton.domain.consultation.entity.SummaryInstruction;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SummaryInstructionRepository extends JpaRepository<SummaryInstruction, Long> {
}
